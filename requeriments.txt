You will need "choice" from the library "random", that already comes with python, so you don't have to install
You will need "sleep" from the library "time", that already comes with python, so you don't have to install
You will need "load" from the library "json", that already comes with python, so you don't have to install
